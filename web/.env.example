# Copy this file to web/.env.local (which is gitignored) and fill in your values.
# Both come from Supabase Dashboard → Settings → API.
#
# The ANON key (not the service_role key!) belongs here. It is safe to ship in
# the browser bundle because Row Level Security enforces access on the server.

VITE_SUPABASE_URL=https://oggpadhkqwtohnjngbxe.supabase.co
VITE_SUPABASE_ANON_KEY=<your-anon-public-key>
