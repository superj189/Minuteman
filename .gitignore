# ── Sensitive voter data — NEVER commit PII to GitHub ──────────────
# The SOS voter file, merged CSV, and dashboard all contain personal data.
# NOTE: scoped to the repo-root /data/ folder so it does NOT catch the app's
# own web/src/data/ (district boundary, etc.).
/data/
*.csv
*.tsv
hd100_dashboard.html
targeted_voters.csv

# ── Secrets ────────────────────────────────────────────────────────
.env
.env.*
!.env.example

# ── Node / build ───────────────────────────────────────────────────
node_modules/
dist/
.next/
build/

# ── Supabase CLI ───────────────────────────────────────────────────
supabase/.temp/
supabase/.branches/

# ── Local-only dev/test tooling (throwaway creds, screenshots) ──────
web/shots/
web/shoot.mjs
scripts/make_test_user.mjs
scripts/create_demo_data.mjs
scripts/clear_demo_data.mjs

# ── OS / editor ────────────────────────────────────────────────────
.DS_Store
Thumbs.db
.vscode/
.idea/
